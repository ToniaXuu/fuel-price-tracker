# 2026-06-18

## 项目初始化：纯静态 + GitHub Actions 自动更新架构

- 将 `index.html` 中硬编码的 `jinanData` 数组抽离为 `data.json`
- 重写 `index.html` 全部 JS 逻辑为 fetch JSON 动态驱动：
  - Hero stats、当前油价、快速统计、数据表格、折线图、柱状图全部从 data.json 计算
  - Y 轴范围动态计算
  - 最近连续变动（连涨/连降）自动检测
  - 城市名、年份、更新日期从 meta 读取
- 创建 `scripts/update_prices.py`：三级数据源（MOFCOM > 东方财富 > 团友网），NDRC 标准换算公式，数据验证
- 创建 `.github/workflows/update-prices.yml`：每日 09:00 自动运行，有变更自动 commit
- 更新 README 说明架构
- 测试通过：dry-run 正确识别无新数据
- 推送到 GitHub：index.html/data.json/scripts/update_prices.py/README.md 已推送成功
- GitHub Actions workflow 文件因 PAT 缺少 `workflow` scope 暂未推送，用户更新 token 权限后推送成功
- 修复 Actions 警告：checkout v4→v5, setup-python v5→v6
- 优化图表：Chart.js → ECharts，走势图新增 markLine 峰值线 + markPoint 关键事件标注（峰值/首降/最大涨幅）+ dataZoom 缩放 + 工具箱保存图片
- 数据容错：新增 FALLBACK_DATA 内嵌回退 + 重试按钮
