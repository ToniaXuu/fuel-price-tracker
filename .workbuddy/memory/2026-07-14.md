# 2026-07-14 工作日志

## 数据获取链路全面修复

### 根因诊断
- **update_prices.py key 名不匹配**: 脚本读 `existing.get("prices", [])`，data.json 实际 key 是 `prices2026` → 始终认为无现有数据，写回 `prices` 新 key，原 `prices2026` 不动
- **东方财富 API 全挂**: 两个报表名 (RPT_ECONOMY_OIL_ADJUST / RPT_ECONOMY_OIL) 均返回 9501 "报表方案不存在"
- **团友网正则不匹配**: 页面实际格式为 `分别降和950元/吨和915元/吨`，原正则期望 `下调 950 元/吨、915元/吨`
- **MOFCOM 延迟**: 商务部网站数据延迟 3-5 天

### 修复清单
1. **update_prices.py**: 读写 key 统一为 `prices2026`，兼容 `prices` 旧格式
2. **团友网正则**: `r'(\d{4})年(\d{1,2})月(\d{1,2})日.*?(降|上调|提高|下调)[^\d]*?(\d+)\s*元/吨[^\d]*?(\d+)\s*元/吨'`
3. **数据源优先级**: 团友网(首位，更新最及时) → MOFCOM(合并去重) → 东方财富(备用)
4. **data.json**: 手动添加第13轮（7月3日下调 950/915 元/吨，p92=7.15）

## 首次加载竞态修复
- ECharts 用 `defer` 从 CDN 加载 → 内联 `init()` 执行更早 → `echarts.init` 抛异常
- 修复: `init()` 包装在 `DOMContentLoaded` 中（defer 脚本在此前执行完毕）

## FALLBACK_DATA 同步
- 添加第13轮数据，lastUpdated 更新为 `2026-07-03`

## 部署状态
- 本地 commit `3aa9034` 已就绪，含所有修复
- GitHub push 被墙（HTTPS/SSH 均不通）
- 需手动推送或配置代理后推送
